import { AutomationLayout } from "@/components/automation/AutomationLayout";

export default function AutomationSectionLayout({ children }: { children: React.ReactNode }) {
  return <AutomationLayout>{children}</AutomationLayout>;
}
