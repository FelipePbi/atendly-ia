# Prompt copiado do Claude Design

Origem: opção **Hand off to Claude Code → Local agent**, com `Atendly.dc.html` como arquivo em foco.

O bloco abaixo é preservado fielmente como artefato da exportação. A instrução de implementação só deve ser executada em uma tarefa futura, depois da leitura das fontes soberanas do repositório.

```text
Use the claude_design MCP (https://api.anthropic.com/v1/design/mcp, auth via /design-login) to import this project:
https://claude.ai/design/p/082dcda3-c866-43ce-9f94-6509e30696d3?file=Atendly.dc.html

Focus on these files (the whole project is readable):
- `Atendly.dc.html`

Also read these files the selection imports:
- `support.js`

Implement: `Atendly.dc.html`
```
